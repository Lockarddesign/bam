---
/**
 * Residential Service Template — /residential/[slug]/
 *
 * Generates 5 service pages from siteConfig.residential.services. Design
 * language mirrors the commercial
 * service / homepage patterns: uppercase H1, tight tracking, commercial card
 * geometry, border-left FAQ, two-column bottom CTA with embedded form. Lighter
 * sage-and-white palette.
 */
import ResidentialLayout from '../../layouts/ResidentialLayout.astro';
import ResidentialServiceSchema from '../../components/schema/ResidentialServiceSchema.astro';
import { siteConfig } from '../../config/site.config';

export async function getStaticPaths() {
  return siteConfig.residential.services.map((service) => ({
    params: { slug: service.slug },
    props: { service },
  }));
}

const { service } = Astro.props;
const res = siteConfig.residential;
const canonicalURL = new URL(`/residential/${service.slug}/`, Astro.site).toString();

const breadcrumbs = [
  { name: 'Residential', url: '/residential/' },
  { name: service.title, url: `/residential/${service.slug}/` },
];

// Per service data comes from the config. service.included, service.faq, and
// the shared res.process drive the page. No repair, install, or symptom copy.
const included = service.included;
const process = res.process;
const faq = service.faq;
---

<ResidentialLayout
  title={`${service.title} in Schuylkill County, PA | Building and Air`}
  description={`${service.title} for homes in Schuylkill County, PA. The only NADCA certified company in the county. Preventive duct cleaning and indoor air quality care. Call (570) 900-5827.`}
  canonicalURL={canonicalURL}
>
  <ResidentialServiceSchema
    slot="head"
    canonicalURL={canonicalURL}
    serviceName={service.title}
    serviceDescription={service.shortDescription}
    breadcrumbs={breadcrumbs}
    faq={faq}
  />

  <!-- ═══════════════════════════════════════════════════════════════
       HERO — mirror commercial homepage hero composition
       ═══════════════════════════════════════════════════════════════ -->
  <section class="relative w-full py-20 md:py-0 md:h-[90vh] md:min-h-[760px] flex flex-col items-center justify-center font-sans overflow-hidden res-bg-warm">
    <div class="absolute inset-0 z-0">
      <img
        src="https://images.unsplash.com/photo-1600210492493-0946911123ea?auto=format&fit=crop&w=1440&q=60"
        alt={`${service.title} for homes`}
        class="w-full h-full object-cover opacity-25"
        loading="eager"
        fetchpriority="high"
        decoding="async"
      />
      <div class="absolute inset-0" style="background: linear-gradient(to bottom, rgba(255,255,255,0.82) 0%, rgba(255,255,255,0.62) 50%, rgba(255,255,255,0.95) 100%);"></div>
      <div class="absolute inset-0 opacity-[0.04] site-grid-light"></div>
    </div>

    <div class="relative z-10 w-full max-w-4xl mx-auto px-6 flex flex-col items-center text-center">
      <nav class="text-[11px] res-text-slate mb-5 md:mb-6 flex items-center gap-2 flex-wrap justify-center font-semibold tracking-wider uppercase" aria-label="Breadcrumb">
        <a href="/residential/" class="hover:opacity-70 res-text-slate no-underline transition-opacity">Residential</a>
        <span class="opacity-50">/</span>
        <span class="res-text-ink">{service.title}</span>
      </nav>

      <div class="mb-4 md:mb-7 flex items-center gap-3 animate-fade-in-up opacity-0" style="animation-delay: 0ms;">
        <div class="w-2 h-2 rounded-full bg-[var(--color-sage-deep)] animate-pulse"></div>
        <span class="font-bold text-[10px] md:text-xs tracking-[0.3em] uppercase res-text-sage">For Homeowners</span>
      </div>

      <h1 class="text-[2rem] md:text-[3.5rem] lg:text-[4.5rem] font-black leading-[0.88] tracking-tighter res-text-ink mb-6 md:mb-10 animate-fade-in-up opacity-0 uppercase drop-shadow-sm" style="animation-delay: 100ms;">
        {service.title}<br class="hidden md:inline" />
        <span class="res-text-sage">in Schuylkill County.</span>
      </h1>

      <p class="text-[14px] md:text-lg res-text-slate max-w-2xl leading-relaxed font-light mb-8 md:mb-14 opacity-0 animate-fade-in-up" style="animation-delay: 200ms;">
        {service.heroSub}
      </p>

      <div class="flex flex-col sm:flex-row items-center gap-4 animate-fade-in-up opacity-0 w-full justify-center" style="animation-delay: 300ms;">
        <a
          href="#service-quote"
          class="res-btn-primary inline-flex items-center justify-center gap-3 font-bold tracking-widest text-xs py-4 md:py-[18px] px-8 md:px-10 rounded-sm w-full sm:w-auto sm:min-w-[240px] uppercase no-underline"
        >
          {res.ctaLabel}
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
        <a
          href={`tel:${res.phoneRaw}`}
          class="hidden sm:flex items-center justify-center gap-3 px-10 py-[18px] bg-white border-2 res-text-ink font-bold text-xs tracking-widest uppercase hover:bg-[var(--color-sage-deep)] hover:text-white transition-all rounded-sm min-w-[240px] shadow-md no-underline group"
          style="border-color: var(--color-sage-deep);"
        >
          Call {res.phone}
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="res-text-sage group-hover:text-white transition-colors"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       WHAT IS INCLUDED — commercial-style cards on light bg
       ═══════════════════════════════════════════════════════════════ -->
  <section class="relative res-bg-cream py-14 md:py-18 overflow-hidden">
    <div class="absolute inset-0 opacity-[0.04] site-grid-light"></div>

    <div class="relative z-10 max-w-5xl mx-auto px-6 md:px-10">
      <div class="text-center mb-12 md:mb-14">
        <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
          What you get
        </span>
        <h2 class="text-4xl md:text-5xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
          What Is Included.
        </h2>
        <div class="w-20 h-1 mx-auto opacity-40 rounded-full" style="background-color: var(--color-sage-deep);"></div>
      </div>
      <ul class="grid grid-cols-1 md:grid-cols-2 gap-5">
        {included.map((item) => (
          <li class="relative rounded-xl p-6 bg-white border border-[var(--color-warm-line)] flex items-start gap-4 transition-all duration-300 hover:-translate-y-1 hover:border-[var(--color-sage-deep)]/30 hover:shadow-[0_14px_30px_-12px_rgba(38,90,74,0.14)]">
            <span class="w-8 h-8 rounded-lg flex items-center justify-center flex-shrink-0" style="background-color: rgba(167, 215, 197, 0.4); color: var(--color-sage-deep);">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
            </span>
            <span class="text-base md:text-lg res-text-ink leading-snug font-semibold">{item}</span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  {service.id === 'maintenance-plans' && (
    <!-- ═══════════════════════════════════════════════════════════════
         MAINTENANCE PLAN TIERS — Good / Better / Best
         ═══════════════════════════════════════════════════════════════ -->
    <section class="relative res-bg-warm py-14 md:py-18 overflow-hidden">
      <div class="absolute inset-0 opacity-[0.04] site-grid-dots"></div>
      <div class="relative z-10 max-w-6xl mx-auto px-6 md:px-10">
        <div class="text-center mb-12 md:mb-14 max-w-2xl mx-auto">
          <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
            Annual plans
          </span>
          <h2 class="text-4xl md:text-5xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
            Good. Better. Best.
          </h2>
          <div class="w-20 h-1 mx-auto opacity-40 rounded-full" style="background-color: var(--color-sage-deep);"></div>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-5 items-stretch">
          {res.plans.map((plan) => (
            <div class={`relative rounded-2xl p-7 md:p-8 flex flex-col h-full transition-all duration-300 hover:-translate-y-1.5 ${plan.featured ? 'bg-[var(--color-sage-deep)] text-white shadow-[0_20px_50px_-16px_rgba(38,90,74,0.4)]' : 'bg-white border border-[var(--color-warm-line)] shadow-[0_2px_16px_rgba(38,90,74,0.06)] hover:shadow-[0_16px_40px_rgba(38,90,74,0.14)]'}`}>
              {plan.featured && (
                <span class="absolute top-5 right-5 text-[9px] font-bold tracking-[0.18em] uppercase px-2.5 py-1 rounded-full bg-white/20 text-white">Most complete</span>
              )}
              <div class={`text-[11px] font-bold tracking-[0.22em] uppercase mb-2 ${plan.featured ? 'text-white/80' : 'res-text-sage'}`}>{plan.tagline}</div>
              <h3 class={`text-2xl md:text-3xl font-black uppercase tracking-tight mb-3 ${plan.featured ? 'text-white' : 'res-text-ink'}`}>{plan.name}</h3>
              <p class={`text-sm leading-relaxed mb-5 ${plan.featured ? 'text-white/85' : 'res-text-slate'}`}>{plan.summary}</p>
              <ul class="space-y-3 mb-6 flex-grow">
                {plan.includes.map((inc) => (
                  <li class="flex items-start gap-3">
                    <span class={`mt-0.5 flex-shrink-0 ${plan.featured ? 'text-white' : 'res-text-sage'}`}>
                      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                    </span>
                    <span class={`text-sm leading-snug ${plan.featured ? 'text-white/90' : 'res-text-ink'}`}>{inc}</span>
                  </li>
                ))}
              </ul>
              <div class={`text-xs font-semibold uppercase tracking-[0.14em] pt-4 border-t ${plan.featured ? 'border-white/20 text-white/80' : 'border-[var(--color-warm-line)] res-text-slate'}`}>{plan.price}</div>
            </div>
          ))}
        </div>
        <p class="text-center text-sm res-text-slate max-w-2xl mx-auto mt-8 leading-relaxed">
          Add ons: dryer vent cleaning and air sampling. A fall preventive maintenance opt in discount is available when you sign with the service agreement.
        </p>
      </div>
    </section>
  )}

  <!-- ═══════════════════════════════════════════════════════════════
       WHY IT MATTERS — preventive value, no symptom list
       ═══════════════════════════════════════════════════════════════ -->
  <section class="res-bg-warm py-14 md:py-18">
    <div class="max-w-6xl mx-auto px-6 md:px-10 grid md:grid-cols-2 gap-10 lg:gap-16 items-start">
      <div>
        <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
          Why it matters
        </span>
        <h2 class="text-4xl md:text-5xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
          Care That Pays<br/>You Back.
        </h2>
        <div class="w-20 h-1 mb-6 rounded-full" style="background-color: var(--color-sage-deep); opacity: 0.4;"></div>
        <p class="text-base md:text-lg leading-relaxed res-text-slate max-w-md">
          We do not sell or replace equipment. Our focus is preventive maintenance that lowers your utility costs and extends the life of the system you already own. A clean system moves air with less resistance, runs more efficiently, and lasts longer.
        </p>
      </div>
      <div class="res-bg-surface border res-border-cream rounded-2xl p-8 md:p-10 shadow-[0_14px_40px_-12px_rgba(38,90,74,0.14)]">
        <p class="text-lg md:text-xl res-text-ink leading-relaxed font-semibold mb-4">
          From secure federal facilities to hospital surgical suites to your living room, the care never changes. Only the address.
        </p>
        <p class="text-base res-text-slate leading-relaxed">
          We are the only NADCA certified company in Schuylkill County. We bring commercial grade restoration and indoor air quality discipline into homes. Source removal, HEPA containment, encapsulation, and lab verified air sampling, held to the same standard we hold in healthcare and federal work.
        </p>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       PROCESS — 4-step, commercial card pattern
       ═══════════════════════════════════════════════════════════════ -->
  <section class="relative res-bg-cream py-14 md:py-18 overflow-hidden">
    <div class="absolute inset-0 opacity-[0.04] site-grid-light"></div>

    <div class="relative z-10 max-w-7xl mx-auto px-6 md:px-10">
      <div class="text-center mb-14 max-w-2xl mx-auto">
        <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
          How it works
        </span>
        <h2 class="text-4xl md:text-5xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
          The NADCA Standard,<br/>Step by Step.
        </h2>
        <div class="w-20 h-1 mx-auto opacity-40 rounded-full" style="background-color: var(--color-sage-deep);"></div>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5">
        {process.map((p) => (
          <div class="group relative rounded-xl p-7 bg-white border border-[var(--color-warm-line)] transition-all duration-300 hover:-translate-y-1.5 hover:border-[var(--color-sage-deep)]/30 hover:shadow-[0_14px_30px_-12px_rgba(38,90,74,0.14)] overflow-hidden">
            <div class="absolute inset-x-0 top-0 h-[3px] bg-gradient-to-r from-[var(--color-sage)] to-[var(--color-sage-deep)] opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <div class="text-3xl font-black res-text-sage mb-3 tracking-tight">{p.num}</div>
            <h3 class="font-extrabold text-lg uppercase tracking-tight res-text-ink mb-2.5">{p.title}</h3>
            <p class="text-base res-text-slate leading-relaxed">{p.body}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       PRICING POSTURE
       ═══════════════════════════════════════════════════════════════ -->
  <section class="res-bg-warm py-14 md:py-18">
    <div class="max-w-3xl mx-auto px-6 md:px-10 res-bg-surface rounded-2xl p-10 md:p-12 text-center border res-border-cream shadow-[0_14px_40px_-12px_rgba(38,90,74,0.14)]">
      <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
        How we price
      </span>
      <h2 class="text-3xl md:text-4xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
        Straight Prices. Up Front.
      </h2>
      <div class="w-20 h-1 mx-auto mb-6 rounded-full" style="background-color: var(--color-sage-deep); opacity: 0.4;"></div>
      <p class="text-base md:text-lg res-text-slate leading-relaxed">
        Estimates are free. You get clear pricing in writing before we begin, and a written closeout report when we finish. We do not sell or replace equipment, so there is nothing to upsell you in the driveway. No surprise charges at the end of the visit. That is the whole pricing model.
      </p>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       LOCAL TRUST SIGNALS
       ═══════════════════════════════════════════════════════════════ -->
  <section class="res-bg-cream py-14 md:py-18">
    <div class="max-w-5xl mx-auto px-6 md:px-10 text-center">
      <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
        Local and certified
      </span>
      <h2 class="text-4xl md:text-5xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
        We Live Here Too.
      </h2>
      <div class="w-20 h-1 mx-auto mb-6 rounded-full" style="background-color: var(--color-sage-deep); opacity: 0.4;"></div>
      <p class="text-base md:text-lg res-text-slate max-w-2xl mx-auto mb-10 leading-relaxed">
        Building and Air is based in Pine Grove and serves homeowners across Schuylkill County. Our crews carry 20 plus combined years of field experience, and we are the only NADCA certified company in the county. Every job comes with a one year workmanship guarantee and a written statement of cleanliness.
      </p>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 max-w-3xl mx-auto">
        <div class="rounded-xl p-5 bg-white border border-[var(--color-warm-line)]">
          <div class="text-3xl md:text-4xl font-black res-text-sage mb-1 tracking-tight">20+</div>
          <div class="text-xs font-bold tracking-[0.14em] uppercase res-text-slate">Combined years experience</div>
        </div>
        <div class="rounded-xl p-5 bg-white border border-[var(--color-warm-line)]">
          <div class="text-3xl md:text-4xl font-black res-text-sage mb-1 tracking-tight">NADCA</div>
          <div class="text-xs font-bold tracking-[0.14em] uppercase res-text-slate">Certified, the only one in county</div>
        </div>
        <div class="rounded-xl p-5 bg-white border border-[var(--color-warm-line)]">
          <div class="text-3xl md:text-4xl font-black res-text-sage mb-1 tracking-tight">1 yr</div>
          <div class="text-xs font-bold tracking-[0.14em] uppercase res-text-slate">Workmanship guarantee</div>
        </div>
        <div class="rounded-xl p-5 bg-white border border-[var(--color-warm-line)]">
          <div class="text-3xl md:text-4xl font-black res-text-sage mb-1 tracking-tight">Free</div>
          <div class="text-xs font-bold tracking-[0.14em] uppercase res-text-slate">Estimates, no pressure</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       SERVICE AREA
       ═══════════════════════════════════════════════════════════════ -->
  <section class="res-bg-warm py-14 md:py-18">
    <div class="max-w-5xl mx-auto px-6 md:px-10 text-center">
      <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
        Where we work
      </span>
      <h2 class="text-4xl md:text-5xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
        Towns We Serve.
      </h2>
      <div class="w-20 h-1 mx-auto mb-10 rounded-full" style="background-color: var(--color-sage-deep); opacity: 0.4;"></div>
      <ul class="flex flex-wrap gap-3 justify-center max-w-3xl mx-auto">
        {res.serviceAreaCities.map((c) => (
          <li>
            <a
              href={`/residential/locations/${c.slug}/`}
              class="inline-block res-bg-surface border res-border-cream px-4 py-2 rounded-full text-base font-semibold res-text-ink no-underline hover:border-[var(--color-sage-deep)] hover:res-text-sage transition-colors"
            >{c.name}, {c.state}</a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       FAQ — border-left commercial pattern
       ═══════════════════════════════════════════════════════════════ -->
  <section class="relative res-bg-cream py-14 md:py-18 overflow-hidden">
    <div class="absolute inset-0 opacity-[0.04] site-grid-light"></div>

    <div class="relative z-10 max-w-4xl mx-auto px-6 md:px-12 lg:px-16">
      <div class="text-center mb-10 md:mb-12">
        <span class="inline-block font-bold text-[10px] tracking-[0.22em] uppercase mb-4 res-text-sage">
          Questions
        </span>
        <h2 class="text-4xl md:text-5xl lg:text-6xl font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5">
          Frequently Asked Questions
        </h2>
        <div class="w-24 h-1 mx-auto opacity-40 rounded-full" style="background-color: var(--color-sage-deep);"></div>
      </div>
      <div class="space-y-10 md:space-y-12">
        {faq.map((q) => (
          <details class="group border-l-2 pl-6 md:pl-8" style="border-color: rgba(38, 90, 74, 0.4);">
            <summary class="cursor-pointer list-none flex items-start justify-between gap-4">
              <h3 class="text-lg md:text-xl font-bold res-text-ink leading-snug">{q.question}</h3>
              <span class="res-text-sage flex-shrink-0 text-xl leading-none mt-1 transition-transform duration-200 group-open:rotate-45">+</span>
            </summary>
            <p class="res-text-slate text-base md:text-lg leading-relaxed mt-4">{q.answer}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       BOTTOM CTA — two columns, embedded form
       ═══════════════════════════════════════════════════════════════ -->
  <section id="service-quote" class="relative res-bg-warm py-14 md:py-18 overflow-hidden">
    <div class="absolute inset-0 opacity-[0.04] site-grid-dots"></div>

    <div class="relative z-10 max-w-[1440px] mx-auto px-6 md:px-12 lg:px-24">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-start">

        <div class="flex flex-col space-y-7">
          <div>
            <span class="font-bold text-[10px] tracking-[0.22em] uppercase mb-4 block res-text-sage">
              Ready when you are
            </span>
            <h2 class="text-4xl md:text-5xl lg:text-[54px] font-black uppercase tracking-tighter res-text-ink leading-[0.95] mb-5 max-w-xl">
              Get a Free<br/>Estimate.
            </h2>
            <p class="res-text-slate text-base md:text-lg leading-relaxed max-w-xl">
              Tell us about your home and what you are after. A real person calls you back with a straight answer and a free estimate. We typically schedule within a few days, and our hours follow our Google listing.
            </p>
          </div>

          <ul class="space-y-5 mt-1">
            {[
              { title: 'A Real Person Answers', description: 'A neighbor in Pine Grove picks up the phone, no phone tree, no overseas call center.' },
              { title: 'Only NADCA Certified in the County', description: 'We hold the credential no other company in Schuylkill County does, the gold standard for duct cleaning and indoor air quality.' },
              { title: 'Preventive, Not Pushy', description: 'We do not sell or replace equipment. Free estimates, clear pricing, and a written statement of cleanliness on every job.' },
            ].map((item) => (
              <li class="flex items-start gap-4">
                <div class="w-7 h-7 rounded-full flex items-center justify-center mt-1 flex-shrink-0" style="background-color: rgba(167, 215, 197, 0.4); color: var(--color-sage-deep);">
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                </div>
                <div>
                  <p class="res-text-ink text-sm font-bold uppercase tracking-[0.14em]">{item.title}</p>
                  <p class="res-text-slate text-sm md:text-base leading-relaxed mt-1">{item.description}</p>
                </div>
              </li>
            ))}
          </ul>

          <div class="flex items-center gap-3 mt-2">
            <div class="p-2.5 rounded-full" style="background-color: rgba(167, 215, 197, 0.4); color: var(--color-sage-deep);">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            </div>
            <div class="flex flex-col">
              <span class="text-[11px] uppercase tracking-[0.28em] res-text-slate font-semibold">Direct Line</span>
              <a href={`tel:${res.phoneRaw}`} class="text-sm font-bold res-text-ink hover:res-text-sage transition-colors">{res.phone}</a>
            </div>
          </div>
        </div>

        <div class="w-full lg:max-w-xl lg:ml-auto">
          <div class="res-bg-surface border res-border-cream rounded-lg p-6 md:p-8 shadow-[0_24px_60px_-24px_rgba(38,90,74,0.20)]">
            <div class="h-1 w-16 rounded-full mb-4" style="background-color: var(--color-sage-deep);"></div>
            <div class="mb-6">
              <h3 class="res-text-ink font-bold text-base uppercase tracking-[0.12em]">
                Free Estimate Request
              </h3>
              <p class="res-text-slate text-xs md:text-sm mt-2 leading-relaxed">
                Service requested: <span class="font-semibold res-text-ink">{service.title}</span>
              </p>
            </div>

            <form name={`residential-${service.slug}-quote`} method="POST" action="/thank-you/" data-netlify="true" netlify-honeypot="bot-field">
              <input type="hidden" name="form-name" value={`residential-${service.slug}-quote`} />
              <input type="hidden" name="service" value={service.title} />
              <p class="hidden"><label>Don't fill this out: <input name="bot-field" /></label></p>

              <div class="space-y-5">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div class="space-y-1.5">
                    <label for="sv-name" class="res-text-slate text-[10px] font-semibold uppercase tracking-widest">Full Name *</label>
                    <input type="text" id="sv-name" name="name" required placeholder="Jane Smith" class="w-full bg-white border res-border-cream rounded res-text-ink px-4 py-3 text-sm focus:outline-none focus:border-[var(--color-sage-deep)] focus:ring-1 focus:ring-[var(--color-sage-deep)]/20 transition-all placeholder:text-gray-400" />
                  </div>
                  <div class="space-y-1.5">
                    <label for="sv-phone" class="res-text-slate text-[10px] font-semibold uppercase tracking-widest">Phone *</label>
                    <input type="tel" id="sv-phone" name="phone" required placeholder="(570) 555-1234" class="w-full bg-white border res-border-cream rounded res-text-ink px-4 py-3 text-sm focus:outline-none focus:border-[var(--color-sage-deep)] focus:ring-1 focus:ring-[var(--color-sage-deep)]/20 transition-all placeholder:text-gray-400" />
                  </div>
                </div>

                <div class="space-y-1.5">
                  <label for="sv-email" class="res-text-slate text-[10px] font-semibold uppercase tracking-widest">Email</label>
                  <input type="email" id="sv-email" name="email" placeholder="jane@example.com" class="w-full bg-white border res-border-cream rounded res-text-ink px-4 py-3 text-sm focus:outline-none focus:border-[var(--color-sage-deep)] focus:ring-1 focus:ring-[var(--color-sage-deep)]/20 transition-all placeholder:text-gray-400" />
                </div>

                <div class="space-y-1.5">
                  <label for="sv-message" class="res-text-slate text-[10px] font-semibold uppercase tracking-widest">Message</label>
                  <textarea id="sv-message" name="message" rows="4" placeholder={`Tell us what is happening with your system. Service: ${service.title}`} class="w-full bg-white border res-border-cream rounded res-text-ink px-4 py-3 text-sm focus:outline-none focus:border-[var(--color-sage-deep)] focus:ring-1 focus:ring-[var(--color-sage-deep)]/20 transition-all placeholder:text-gray-400 resize-none"></textarea>
                </div>
              </div>

              <div class="mt-6">
                <button type="submit" class="res-btn-primary w-full px-8 py-4 rounded-sm font-bold tracking-[0.22em] uppercase text-sm">
                  {res.ctaLabel}
                </button>
              </div>
            </form>

            <p class="text-[10px] res-text-slate mt-4 leading-relaxed">
              No spam, no pressure pitch, no auto reply chain. A real person reads every message.
            </p>
          </div>
        </div>

      </div>
    </div>
  </section>
</ResidentialLayout>
